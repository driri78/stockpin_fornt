import React from "react";
import { Outlet } from "react-router-dom";

const ChartStockTitle = () => {
  return (
    <div>
      <div>ChartStockTitle</div>
      <Outlet />
    </div>
  );
};

export default ChartStockTitle;
